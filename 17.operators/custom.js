// logical operator
// it gives the ability to check more than one codition 
// && AND (BOTH MUST BE TRUE)
// || OR (EITHER CONDTION CANBE TRUE )

let temp = 50;

if(temp > 0 && temp < 30){
    console.log("the temp is good");

}
else{
    console.log("it is not")
}

// above is the logical operator


//now lets see the not logical operator

// typically used to reverse a condition boolean value
// true -> false false -> true


let ttemp = 34;
if(!(temp > 34)){
    console.log("new temp");
}
